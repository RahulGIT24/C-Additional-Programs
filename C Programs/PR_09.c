//* Display size of diiferent data type
#include <stdio.h>

int main()
{
    int a = 90;
    char c;
    float f1;
    double f2;
    printf("Size of integer in my PC is %d\n", sizeof(a));
    printf("Size of character in my PC is %d\n", sizeof(c));
    printf("Size of float in my PC is %d\n", sizeof(f1));
    printf("Size of double in my PC is %d\n", sizeof(f2));
    return 0;
}